# SPDX-FileCopyrightText: 2026 Noyalib
# SPDX-License-Identifier: MIT OR Apache-2.0

name: shared / Miri (full + big-endian, scheduled)

# Reusable weekly Miri full-sweep. Runs `cargo miri test --lib`
# (with `-Zmiri-disable-isolation`) plus a big-endian cross-target
# run (`mips64-unknown-linux-gnuabi64`) via the caller's
# `scripts/miri.sh`. Catches endianness assumptions that would
# slip past the focused per-PR job.
#
# Bandwidth-intensive so gated behind the `schedule` /
# workflow_dispatch` event. Measured cost on noyalib is ~113 min:
# ~58 min native + ~56 min for the Miri-interpreted big-endian pass.
#
# CADENCE IS THE CALLER'S. This guard only distinguishes schedule from
# push — it cannot tell one cron from another. A consumer that wants
# this weekly rather than nightly must say so at the call site, e.g.
#
#   jobs:
#     miri-full:
#       if: >-
#         github.event_name == 'workflow_dispatch' ||
#         github.event.schedule == '0 5 * * 1'
#       uses: sebastienrousseau/noyalib/.github/workflows/shared-miri-full.yml@<sha>
#
# `github.event.schedule` holds the cron expression that fired. Keep
# multiple crons at different minutes: `cancel-in-progress` keyed on
# workflow + ref means simultaneous crons cancel each other.
#
# CACHE-POISONING GUARD: same isolated CARGO_TARGET_DIR pattern
# as the focused Miri job (target-miri-full for this one).
#
# Consumers pin by SHA:
#
#   jobs:
#     miri-full:
#       uses: sebastienrousseau/noyalib/.github/workflows/shared-miri-full.yml@<sha>

on:
  workflow_call:

permissions:
  contents: read

jobs:
  check:
    name: Miri (full + big-endian, scheduled)
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    # The native full sweep alone measures ~58 min. Until the toolchain
    # fix below, the big-endian pass died within seconds of starting, so
    # the old 90 min budget was never actually tested against a run that
    # got to do the cross work. Now that it does, 90 leaves ~30 min for a
    # Miri-interpreted cross-target suite — too tight to rely on.
    timeout-minutes: 300
    env:
      RUSTUP_TOOLCHAIN: nightly
      CARGO_TARGET_DIR: ${{ github.workspace }}/target-miri-full
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust nightly with Miri
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: nightly
          components: miri
      - name: Cache cargo
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          workspaces: ". -> target-miri-full"
          key: miri-full
          save-if: false
      - name: Setup Miri
        run: cargo +nightly miri setup
      # `gcc-mips64-linux-gnuabi64` alone is not enough: it provides the
      # cross compiler but no target sysroot, so the build falls back to
      # the host `/usr/include` and dies on
      # `bits/libc-header-start.h: No such file or directory`.
      # `libc6-dev-mips64-cross` supplies the mips64 libc headers under
      # /usr/mips64-linux-gnuabi64/include. Both are required.
      - name: Install mips64 cross toolchain (for the big-endian pass)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            gcc-mips64-linux-gnuabi64 \
            libc6-dev-mips64-cross
          mips64-linux-gnuabi64-gcc --version | head -1
          # This step runs BEFORE the ~58-minute native sweep on purpose,
          # so a toolchain or sysroot problem surfaces in seconds instead
          # of an hour in. It costs nothing to front-load: apt is fast and
          # the native pass does not depend on it.
          if [ -f /usr/mips64-linux-gnuabi64/include/bits/libc-header-start.h ]; then
            echo "mips64 sysroot headers present"
          else
            echo "mips64 libc headers missing" >&2
            exit 1
          fi
      - name: Run full Miri suite (native)
        run: cargo +nightly miri test --lib
        env:
          MIRIFLAGS: -Zmiri-disable-isolation
      # `cargo miri test --lib` still builds the crate's dev-dependencies,
      # and criterion pulls `alloca`, whose build script shells out to
      # cc-rs. Cross-compiling to mips64 therefore needs a mips64 C
      # toolchain on the runner even though Miri interprets the Rust and
      # never executes that C. Without it the step dies at
      # `ToolNotFound: mips64-linux-gnuabi64-gcc` before a single test
      # runs. `alloca` is not optional in criterion — it arrives via the
      # default `cargo_bench_support` feature — so this cannot be dropped
      # from the dependency side without breaking the benches.
      # Tree Borrows rejects patterns Stacked Borrows accepts and vice
      # versa; both must pass for a soundness claim. Too slow for the
      # per-PR job, so it runs here on the focused suite.
      - name: Run focused Miri suite (Tree Borrows)
        env:
          MIRI_MODEL: tree
        run: ./scripts/miri.sh
      # The symbolic alignment check makes every multi-byte read prove
      # its alignment, which is what the SWAR decimal parser and the
      # structural bitmask iterator rely on. Combined with Tree Borrows
      # the simd module alone exceeded two hours, so it runs on its own,
      # under Stacked Borrows, on the module that does word-sized reads.
      - name: Run simd module under the symbolic alignment check
        env:
          MIRI_ALIGN: "1"
        run: ./scripts/miri.sh simd
      - name: Run focused Miri suite (big-endian mips64)
        run: ./scripts/miri.sh
        env:
          MIRI_TARGET: mips64-unknown-linux-gnuabi64
          # cc-rs looks up the target-specific compiler by this name.
          CC_mips64_unknown_linux_gnuabi64: mips64-linux-gnuabi64-gcc
